name: billipod
description: "Bill Management with Secure and Private Solid Pod Storage"
publish_to: 'none'
version: 0.1.1+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  gap: ^3.0.1
  intl: ^0.20.2
  provider: ^6.1.5
  solidpod: ^0.12.2
  solidui: ^0.3.18
  uuid: ^4.5.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # assets:
  #   - assets/images/app_icon.png
  #   - assets/images/app_image.jpg
